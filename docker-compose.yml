services:
  postgres:
    image: postgres:16
    container_name: sportflow-postgres
    environment:
      POSTGRES_DB: sportflow
      POSTGRES_USER: sportflow
      POSTGRES_PASSWORD: sportflow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
